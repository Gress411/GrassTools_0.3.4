import win

# import win2
# import tkinter as tk
from tkinter import ttk
# from ttkbootstrap import Style
# import windnd
#
# import os
# import time
# import asyncio
# from ffmpeg import FFmpeg
# import re
import ctypes

if __name__ == '__main__':
    pass

    # 是否开启dpi缩放
    # def click1():
    #     # win2.create_win2()
    #     # style = Style(theme="vapor")
    #     # win.win = style.master
    #     # 告诉操作系统使用程序自身的dpi适配
    #     ctypes.windll.shcore.SetProcessDpiAwareness(1)
    #     # 获取屏幕的缩放因子
    #     ScaleFactor = ctypes.windll.shcore.GetScaleFactorForDevice(0)
    #     # 设置程序缩放
    #     win.win.tk.call('tk', 'scaling', ScaleFactor / 75)
    #
    #
    # b2 = ttk.Button(win.win, text="click", command=click1)
    # b2.pack()

    # ctypes.windll.shcore.SetProcessDpiAwareness(1)
    # # 获取屏幕的缩放因子
    # ScaleFactor = ctypes.windll.shcore.GetScaleFactorForDevice(0)
    # # 设置程序缩放
    # win.win.tk.call('tk', 'scaling', ScaleFactor / 75)
win.win.mainloop()
